# CarFight — 03_VisionAlign

> 역할: CarFight의 **최종 방향 / 현재 기준선 / 현재 구조 갭 / 전환 원칙**을 한 문서에서 고정한다.  
> 마지막 정리(Asia/Seoul): 2026-03-27

---

## 한 줄 정의
CarFight는 **차량 전투와 물리 상호작용**을 중심으로,
다양한 차량을 공통 코어 위에 올리되 최종적으로는
`CMVS / Cluster Union / Geometry Collection` 기반 구조로 가려는 프로젝트다.

---

## 최종 방향
### 1. 차량 전투 중심
- 주행 자체보다 차량 간 충돌, 밀기, 제어 상실, 복구 같은 물리 상호작용이 중요하다.
- 최종적으로는 전투 규칙, 충돌 결과, 카메라, 연출이 차량 상태와 연결되어야 한다.

### 2. 다차종 대응
- 목표는 한 차량 완성이 아니라 여러 차종을 같은 게임 안에 안정적으로 올리는 것이다.
- 차량별 감각 차이는 허용하되, 공통 규칙과 공통 구조를 먼저 확보한다.

### 3. 최종 차량 구조 목표
- 최종 차량 구조 목표는 `CMVS / Cluster Union / Geometry Collection` 기반이다.
- 장기적으로는 부품 조립, 파괴, 분리, 무게 중심 변화까지 구조적으로 열어야 한다.

---

## 현재 기준선
현재 실제 구현은 아래 조합을 기준선으로 본다.

- 현재 기준 플레이 차량: `BP_CFVehiclePawn`
- 현재 기준 Native Pawn: `ACFVehiclePawn`
- 현재 기준 주행 코어: `UCFVehicleDriveComp`
- 현재 기준 휠 시각 동기화 코어: `UCFWheelSyncComp`
- 현재 기준 데이터 축: `UCFVehicleData`
- 현재 기준 테스트 자산: `DA_PoliceCar`

현재 차량 구조는 **CMVS가 아니라**
`AWheeledVehiclePawn + ChaosWheeledVehicleMovementComponent` 기반 하이브리드 구조다.

이 기준은 지금 실제로 굴러가는 기준선이며,
최종 방향을 포기했다는 뜻은 아니다.

---

## 현재 구조 갭
### 1. 맞는 방향
- 판단 / 상태 / 규칙을 C++ 코어에 두는 방향
- `BP_CFVehiclePawn`를 Thin BP로 유지하는 방향
- 차량별 차이를 `VehicleData` 축으로 분리하는 방향
- 단일 차량이 아니라 다차종 확장을 염두에 두는 방향

### 2. 아직 반영되지 않은 방향
- `CMVS` 기반 차량 root 구조
- `Cluster Union` 기반 부품 통합
- `Geometry Collection` 기반 파괴 / 분리
- 에디터 조립형 차량 파이프라인
- 전투 물리 구조를 차량 본체 설계에 직접 반영하는 단계

---

## 지금 유지할 것과 나중에 교체할 것
### 유지 예정 코어
- `UCFVehicleData`
- `DriveState` 코어
- `WheelSync` 코어
- Thin BP 원칙

### 교체 후보 구조
- Vehicle root 구조
- 차량 조립 방식
- 파괴 / 분리 표현 구조
- 최종 전투 물리 반영을 위한 본체 구성 방식

---

## 현재 단계의 의미
지금 단계는 최종 구조에 아직 도달하지 못했더라도,
그 방향으로 갈 때 버리지 않아도 되는 코어를 먼저 닫는 단계다.

정확히는 아래 순서로 본다.
1. 현재 기준선 실측
2. 공통 코어 안정화
3. 유지할 코어와 교체할 구조 분리
4. 최종 구조 전환 설계
5. 실제 전환 작업

---

## 문서 운영 원칙
### 1. 최종 방향과 현재 기준선을 섞지 않는다
- 최종 방향은 이 문서에서 고정한다.
- 현재 실제 상태는 `00_Handover.md`에서 고정한다.

### 2. 임시 편차와 구조 갭을 구분한다
- 예:
  - 입력 장치 문제로 인한 `GamepadOnly`는 임시 편차다.
  - `CMVS 미도입`은 구조 갭이다.

### 3. 테스트 차량을 최종 목표처럼 쓰지 않는다
- `DA_PoliceCar`는 현재 기준선이다.
- 최종 차량 구조의 대체물이 아니다.

---

## 다음 단계에서 열어야 할 주제
- 현재 하이브리드 구조에서 최종 구조로 넘어가는 전환 설계
- `VehicleData / DriveState / WheelSync`를 유지한 채 root 구조를 바꾸는 방법
- 차량 전투 규칙과 파괴 구조를 실제 차량 본체 구조에 연결하는 방법
- 다차종 차량 추가 시 공통 규칙과 차량별 설정의 경계

---

## 이 문서의 사용 방법
- 프로젝트의 방향을 먼저 확인할 때 이 문서를 본다.
- 현재 실제 기준선은 `00_Handover.md`를 본다.
- 작업 순서는 `01_Roadmap.md`를 본다.
- 검증 기준은 `08_P0_Verification.md`를 본다.
- 결정 근거는 `16_CPP_DecisionLog.md`를 본다.
