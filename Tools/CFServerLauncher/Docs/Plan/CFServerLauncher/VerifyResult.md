# CFServerLauncher v1.0 검증 결과

문서 버전: v1.0.10-verify  
작성 기준일: 2026-05-29

## 1. 검증 결론

`CFServerLauncher` v1.0.0 최종 릴리스 검증은 통과했다.

현재 상태:

```text
v1.0.0 최종 릴리스 검증 통과
```

## 2. 통과 확인 항목

| 항목 | 결과 |
|---|---|
| `dotnet build` | 통과 |
| 앱 실행 | 통과 |
| 버전 `v1.0.0` 표시 | 통과 |
| 서버 실행 파일 찾아보기 | 통과 |
| 작업 폴더 찾아보기 | 통과 |
| 로그 파일 찾아보기 | 통과 |
| 서버 시작 | 통과 |
| TestMap 로그 표시 | 통과 |
| 현재 실행 로그 파일 경로 표시 | 통과 |
| 실행 로그 파일명 `server_yyyyMMdd_HHmmss_fff.log` 형식 | 통과 |
| 기준 로그 경로 `server.log` 유지 | 통과 |
| 설정 저장 시 timestamp 로그 파일명 미저장 | 통과 |
| 서버 종료 버튼 종료 시 상태 `중지됨` | 통과 |
| 외부 종료 시 상태 `종료됨` | 통과 |
| 종료 후 재시작 | 통과 |
| 서버 실행 중 창 닫기 확인 대화상자 | 통과 |
| `publish.ps1` 실행 | 통과 |
| `publish.ps1` 한글 출력 | 통과 |
| `Publish\CFServerLauncher.exe` 생성 | 통과 |
| `Server`, `Config`, `Logs` 폴더 유지 | 통과 |
| `Publish/` Git 제외 | 통과 |

## 3. 최종 완료된 v1.0 기능

- 설정 JSON 저장/불러오기
- 서버 EXE 경로 관리
- 작업 폴더 관리
- 맵 경로 관리
- 포트 관리
- 로그 기준 경로 관리
- 현재 실행 로그 파일 경로 표시
- 실행 인자 미리보기
- 서버 시작
- 서버 종료
- 실행 상태 표시
- PID 표시
- 종료 코드 표시
- 최근 로그 tail 표시
- 찾아보기 버튼
- 로그 파일 잠금 회피용 timestamp 로그 파일 생성
- 수동 종료와 외부 종료 상태 구분
- 서버 실행 중 런처 닫기 확인 대화상자
- 최종 버전 표시
- publish 스크립트

## 4. 최종 판정

```text
CFServerLauncher v1.0.0 완료
```

## 5. 다음 단계

v1.1 계획으로 넘어간다.

우선 후보:

```text
1. 실행 프로필
2. 로그 파일 정리 기능
3. 서버 빌드 교체 절차 정리
4. 간단 CPU/메모리 표시
5. 로그 검색/필터
```
